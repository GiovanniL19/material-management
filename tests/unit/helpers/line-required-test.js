
import { lineRequired } from 'material-management/helpers/line-required';
import { module, test } from 'qunit';

module('Unit | Helper | line required');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lineRequired([42]);
  assert.ok(result);
});

